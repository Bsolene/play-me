
  <%= simple_form_for @game do |f| %>
    <%= f.input :name, collection: Game::GAMES.keys.sort, label: 'Game' %>
    <%= f.input :stake %>
    <%= f.button :submit, class: 'btn-success' %>
  <% end %>

